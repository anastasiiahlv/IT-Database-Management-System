<UserControl x:Class="DatabaseDesktopClient.Views.DatabaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <StackPanel>
        <TextBlock Text="{Binding DatabaseName}" 
                  FontSize="32" 
                  FontWeight="Bold"
                  Margin="0,0,0,20"/>

        <TextBlock Text="{Binding WelcomeMessage}"
                  FontSize="18"
                  Foreground="#616161"
                  Margin="0,0,0,30"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" 
                   Background="#E3F2FD" 
                   Padding="20"
                   Margin="0,0,10,0"
                   CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="📊 Статистика" 
                              FontSize="18" 
                              FontWeight="Bold"
                              Margin="0,0,0,15"/>
                    <TextBlock>
                        <Run Text="Таблиць: " FontWeight="SemiBold"/>
                        <Run Text="{Binding TableCount}"/>
                    </TextBlock>
                    <TextBlock Margin="0,5,0,0">
                        <Run Text="Рядків: " FontWeight="SemiBold"/>
                        <Run Text="{Binding TotalRowCount}"/>
                    </TextBlock>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" 
                   Background="#F3E5F5" 
                   Padding="20"
                   Margin="10,0,0,0"
                   CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="ℹ️ Інформація" 
                              FontSize="18" 
                              FontWeight="Bold"
                              Margin="0,0,0,15"/>
                    <TextBlock>
                        <Run Text="Створено: " FontWeight="SemiBold"/>
                        <Run Text="{Binding CreatedAt}"/>
                    </TextBlock>
                    <TextBlock Margin="0,5,0,0">
                        <Run Text="Змінено: " FontWeight="SemiBold"/>
                        <Run Text="{Binding ModifiedAt}"/>
                    </TextBlock>
                    <TextBlock Margin="0,5,0,0" TextWrapping="Wrap">
                        <Run Text="Файл: " FontWeight="SemiBold"/>
                        <Run Text="{Binding FilePath}"/>
                    </TextBlock>
                </StackPanel>
            </Border>
        </Grid>
    </StackPanel>
</UserControl>